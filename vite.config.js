// import { resolve } from 'path';
// const glsl = require('vite-plugin-glsl');
// import { defineConfig } from 'vite';

export default {
  // plugins: [glsl()],

  resolve: {
    alias: {
      // '~': resolve(__dirname, './'),
    },
  },
};
